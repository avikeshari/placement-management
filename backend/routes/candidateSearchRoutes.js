const express=require('express'); const protect=require('../middleware/authMiddleware'); const authorize=require('../middleware/roleMiddleware'); const c=require('../controllers/candidateSearchController'); const r=express.Router(); r.use(protect,authorize('company')); r.get('/',c.companySearch); r.get('/:studentId/notes',c.candidateNotes); r.post('/:studentId/notes',c.addCandidateNote); module.exports=r;
