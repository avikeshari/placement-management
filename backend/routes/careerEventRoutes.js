const express=require('express'); const protect=require('../middleware/authMiddleware'); const authorize=require('../middleware/roleMiddleware'); const c=require('../controllers/careerEventController'); const r=express.Router(); r.use(protect); r.get('/',authorize('student','company','admin'),c.listEvents); r.post('/',authorize('admin'),c.createEvent); r.post('/:id/register',authorize('student','company'),c.registerEvent); r.delete('/:id/register',authorize('student','company'),c.unregisterEvent); module.exports=r;
