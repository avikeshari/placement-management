const express=require('express'); const protect=require('../middleware/authMiddleware'); const authorize=require('../middleware/roleMiddleware'); const c=require('../controllers/companyFollowController'); const r=express.Router(); r.use(protect,authorize('student')); r.get('/',c.myFollows); r.post('/:companyId',c.followCompany); r.delete('/:companyId',c.unfollowCompany); module.exports=r;
