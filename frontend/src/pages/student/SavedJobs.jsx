import {useEffect,useState} from "react"; import api from "../../api/axios"; import Loader from "../../components/Loader"; import toast from "react-hot-toast";
export default function SavedJobs(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setRows((await api.get("/saved-jobs")).data.savedJobs||[])}catch(e){toast.error(e.response?.data?.message||"Unable to load saved jobs")}finally{setLoading(false)}};useEffect(()=>{load()},[]);const remove=async(id)=>{await api.delete(`/saved-jobs/${id}`);load()};if(loading)return <Loader text="Loading saved jobs..."/>;return <section><h1 className="text-3xl font-bold mb-6">Saved Jobs</h1>{!rows.length?<p className="text-slate-500">No saved jobs yet.</p>:<div className="space-y-4">{rows.map(r=><div className="bg-white border rounded-2xl p-5 flex justify-between gap-4" key={r._id}><div><h2 className="font-semibold">{r.job?.title}</h2><p className="text-slate-500">{r.job?.company?.name} · {r.job?.location||"Location flexible"}</p></div><button onClick={()=>remove(r.job?._id)} className="text-red-600">Remove</button></div>)}</div>}</section>}
