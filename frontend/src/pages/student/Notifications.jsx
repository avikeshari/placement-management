import {useEffect,useState} from "react";import api from "../../api/axios";import Loader from "../../components/Loader";
export default function Notifications(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setItems((await api.get("/notifications")).data.notifications||[])}catch(e){}finally{setLoading(false)}};useEffect(()=>{load()},[]);const read=async(id)=>{await api.patch(`/notifications/${id}/read`);load()};if(loading)return <Loader text="Loading notifications..."/>;return <section><h1 className="text-3xl font-bold mb-6">Notifications</h1><div className="space-y-3">{items.map(n=><button onClick={()=>read(n._id)} key={n._id} className={`w-full text-left bg-white border rounded-xl p-4 ${!n.readAt?'border-blue-300':''}`}><p className="font-semibold">{n.title}</p><p className="text-slate-600 mt-1">{n.message}</p><p className="text-xs text-slate-400 mt-2">{new Date(n.createdAt).toLocaleString()}</p></button>)}</div></section>}
