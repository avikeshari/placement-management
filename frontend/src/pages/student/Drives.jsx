import {useEffect,useState} from "react";import toast from "react-hot-toast";import api from "../../api/axios";export default function Drives(){const[d,setD]=useState([]);const load=async()=>{try{const r=await api.get("/drives");setD(r.data.drives||[])}catch(e){toast.error("Unable to load placement drives")}};useEffect(()=>{load()},[]);return <section><h1 className="text-2xl md:text-3xl font-bold">Placement Drives</h1><p className="text-slate-500 mt-2">Join placement drives and track participation.</p><div className="space-y-4 mt-6">{d.map(x=><article key={x._id} className="bg-white border rounded-2xl p-5"><h2 className="font-semibold text-lg">{x.name}</h2><p className="text-slate-500 mt-1">{new Date(x.startAt).toLocaleString("en-IN")} – {new Date(x.endAt).toLocaleString("en-IN")}</p><p className="mt-3">{x.description}</p><button onClick={async()=>{try{await api.post(`/drives/${x._id}/join`);toast.success("You joined the placement drive");load()}catch(e){toast.error(e.response?.data?.message||"Unable to join drive")}}} className="mt-4 bg-blue-600 text-white px-4 py-2 rounded-lg">Join Drive</button></article>)}</div></section>}