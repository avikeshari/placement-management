import {useEffect,useState} from 'react';import api from '../../api/axios';import toast from 'react-hot-toast';import Loader from '../../components/Loader';
export default function Verification(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setRows((await api.get('/admin/companies')).data.companies||[])}catch(e){toast.error(e.response?.data?.message||'Unable to load companies')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const toggle=async(c)=>{try{await api.patch(`/admin/companies/${c._id}/verification`,{verified:!c.isVerified});toast.success('Verification updated');load()}catch(e){toast.error(e.response?.data?.message||'Unable to update verification')}};if(loading)return <Loader text="Loading companies..."/>;return <section><h1 className="text-3xl font-bold mb-6">Employer Verification</h1><div className="space-y-3">{rows.map(c=><div className="bg-white border rounded-xl p-4 flex justify-between" key={c._id}><div><p className="font-semibold">{c.name}</p><p className="text-slate-500">{c.email}</p></div><button onClick={()=>toggle(c)} className={c.isVerified?'text-red-600':'text-green-600'}>{c.isVerified?'Remove Verification':'Verify Company'}</button></div>)}</div></section>}
