import {useCallback,useEffect,useState} from 'react';import toast from 'react-hot-toast';import api from '../../api/axios';import Loader from '../../components/Loader';import getErrorMessage from '../../utils/getErrorMessage';
export default function Drives(){const[drives,setDrives]=useState([]),[loading,setLoading]=useState(true),[form,setForm]=useState({name:'',description:'',startAt:'',endAt:'',location:''});const load=useCallback(async()=>{try{setLoading(true);const r=await api.get('/drives');setDrives(r.data.drives||[])}catch(e){toast.error(getErrorMessage(e,'Unable to load drives.'))}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);const create=async e=>{e.preventDefault();if(!form.name.trim()||!form.startAt||!form.endAt){toast.error('Drive name, start date and end date are required.');return}if(new Date(form.endAt)<=new Date(form.startAt)){toast.error('End date/time must be later than the start date/time.');return}try{await api.post('/drives',form);toast.success('Placement drive created');setForm({name:'',description:'',startAt:'',endAt:'',location:''});load()}catch(e){toast.error(getErrorMessage(e,'Unable to create drive.'))}};if(loading)return <Loader text="Loading placement drives..."/>;return <section><h1 className="text-2xl md:text-3xl font-bold">Placement Drives</h1><p className="text-slate-500 mt-2">Organize drives, coordinate companies and track participation.</p><form onSubmit={create} className="bg-white border rounded-2xl p-6 mt-6 grid md:grid-cols-2 gap-4"><input required placeholder="Drive name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="border rounded-lg px-3 py-2.5"/><input placeholder="Location / venue" value={form.location} onChange={e=>setForm({...form,location:e.target.value})} className="border rounded-lg px-3 py-2.5"/><label>Start<input required type="datetime-local" value={form.startAt} onChange={e=>setForm({...form,startAt:e.target.value})} className="block w-full border rounded-lg px-3 py-2.5"/></label><label>End<input required type="datetime-local" min={form.startAt || undefined} value={form.endAt} onChange={e=>setForm({...form,endAt:e.target.value})} className="block w-full border rounded-lg px-3 py-2.5"/></label><textarea placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="md:col-span-2 border rounded-lg px-3 py-2.5"/><button className="md:col-span-2 bg-blue-600 text-white rounded-lg px-5 py-2.5">Create Placement Drive</button></form><div className="space-y-4 mt-6">{drives.map(d=><article key={d._id} className="bg-white border rounded-2xl p-5"><div className="flex justify-between"><div><h2 className="font-semibold text-lg">{d.name}</h2><p className="text-slate-500">{new Date(d.startAt).toLocaleString('en-IN')} – {new Date(d.endAt).toLocaleString('en-IN')}</p></div><span className="capitalize bg-blue-50 text-blue-700 px-3 py-1 rounded-full h-fit">{d.status}</span></div><p className="mt-3">Participants: <strong>{d.participants?.length||0}</strong> · Companies: <strong>{d.companies?.length||0}</strong></p></article>)}</div></section>}
