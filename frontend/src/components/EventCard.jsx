import { CalendarDays } from 'lucide-react';
export default function EventCard({event,onRegister,onUnregister,registered=false}){return <article className="bg-white border rounded-2xl p-5"><div className="flex gap-3"><CalendarDays className="text-blue-600"/><div><h2 className="font-semibold">{event.title}</h2><p className="text-sm text-slate-500">{new Date(event.startAt).toLocaleString()} · {event.location||'Online'}</p></div></div><p className="text-slate-600 mt-3">{event.description}</p><div className="mt-4 flex gap-2">{registered?<button onClick={()=>onUnregister?.(event._id)} className="border rounded-lg px-4 py-2">Cancel registration</button>:<button onClick={()=>onRegister?.(event._id)} className="bg-blue-600 text-white rounded-lg px-4 py-2">Register</button>}</div></article>}
